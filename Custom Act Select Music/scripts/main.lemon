function u64 Standalone.getModdedSoundKey(u64 soundKey, u8 sfxId, u8 soundRegType)
{
    if (soundKey == MUSIC_DATASELECT && global.game_mode == 0x00)
        return "act_select"
    else
        return base.Standalone.getModdedSoundKey(soundKey, sfxId, soundRegType)
}
